<% if(current.path.length > 1){ %>
<ol class="breadcrumb">
    <li><a href="/">Home</a></li>
    <% for(var i in current.path){ %>
    <li <%- current.path[i] === current.source ? 'class="active"' : '' %>>
        <a href="/<%=current.path[i] %>"><%=: current.path[i] | capitalize %></a>
    </li>
    <% } %>
</ol>
<% } %>

